[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn mysite.wsgi --workers 2 --threads 4 --timeout 60 --log-file -"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
